<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:interfaces="clr-namespace:HandyCrab.Common.Interfaces;assembly=HandyCrab.Common"
             xmlns:ui="clr-namespace:HandyCrab.UI;assembly=HandyCrab.UI"
             mc:Ignorable="d"
             BindingContext="{x:Static ui:ModelViewLocator.LoginViewModel}"
             x:DataType="{x:Type interfaces:ILoginViewModel}"
             x:Class="HandyCrab.UI.Views.LoginPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="{Binding UserNamePlaceholder}" Text="{Binding UserName}"></Entry>
            <Entry Placeholder="{Binding PasswordPlaceholder}" IsPassword="True" Text="{Binding Password}"></Entry>
            <Button x:Name="LoginButton" Text="{Binding LoginButtonText}" Command="{Binding LoginCommand}" ></Button>
            <Button x:Name="NoAccountButton" Text="{Binding NoAccountButtonText}" Clicked="NoAccountButton_Clicked" ></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>